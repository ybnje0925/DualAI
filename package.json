{
  "name": "dualai-web",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "test": "node --test tests-web/*.test.mjs"
  },
  "engines": {
    "node": "24.x"
  }
}
