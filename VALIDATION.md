# MVP 검증 결과

- Windows 실행 파일 빌드 성공 (약 48 MiB).
- 실제 설치된 Microsoft Edge 엔진에서 어댑터 테스트 9개 통과.
- Tk 입력창 및 재시도 상태 테스트 4개 통과.
- 한글·영문·코드·특수문자·연속 빈 줄·긴 질문의 입력 보존 확인.
- 한쪽 실패 격리, 작성 중인 초안 보존, 생성 중 전송 차단 확인.
- 성공한 쪽 중복 재전송 방지, 실패 시 원래 질문 유지, 불확실한 전송 잠금 확인.
- 일반 브라우저 실행 경로에서 ChatGPT와 Gemini 입력창 표시 확인.
- 서로 다른 브라우저 창 ID와 좌우 배치 확인.
- 테스트 쿠키를 이용한 전용 프로필의 재시작 후 유지 확인.
- 패키징된 exe 실행 시 네이티브 입력창의 응답과 브라우저 연결 확인.

## 사용자 계정으로 남은 확인

1. 각 서비스의 로그인 및 필요한 쿠키/보안 확인을 직접 완료합니다.
2. `오늘 서울 날씨 알려줘`를 입력하고 `둘 다 전송`을 누릅니다.
3. 두 서비스 모두 동일한 질문을 받고 답변하는지 확인합니다.
4. 앱을 닫았다가 다시 열어 실제 계정 로그인이 유지되는지 확인합니다.

실제 유료 계정 로그인과 실제 AI 답변 수신은 자동 검증하지 않았습니다.
사이트 정책과 화면 변경에 따라 어댑터 조정이 필요할 수 있습니다.

## 재현

```powershell
.venv/Scripts/python.exe -m unittest discover -s tests -v
.venv/Scripts/python.exe -u -m scripts.smoke_native
```

두 번째 명령은 실제 서비스 화면을 열고 합성 테스트 쿠키를 확인합니다.
질문을 전송하거나 비밀번호를 입력하지 않습니다.
검증용 프로필과 화면 캡처는 `artifacts/`에만 생성됩니다.
